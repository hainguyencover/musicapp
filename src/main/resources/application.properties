spring.application.name=musicapp
# --- Database Configuration ---
# Thay ??i username, password, và tên database c?a b?n
spring.datasource.url=jdbc:mysql://localhost:3306/music_app_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=123456

# --- JPA/Hibernate Configuration ---
# T? ??ng c?p nh?t schema (dùng 'validate' ho?c 'none' trên production)
spring.jpa.hibernate.ddl-auto=update
# Hi?n th? câu l?nh SQL trong log (h?u ích khi dev)
spring.jpa.show-sql=true 
# Format câu l?nh SQL cho d? ??c (tùy ch?n)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Thymeleaf Configuration ---
# T?t cache Thymeleaf khi dev ?? th?y thay ??i HTML ngay l?p t?c
spring.thymeleaf.cache=false 
# ???ng d?n t?i th? m?c view
spring.thymeleaf.prefix=classpath:/templates/ 
spring.thymeleaf.suffix=.html

# --- File Upload Configuration ---
spring.servlet.multipart.enabled=true
# Gi?i h?n kích th??c file upload t?ng th?
spring.servlet.multipart.max-file-size=50MB
# Gi?i h?n kích th??c m?i file
spring.servlet.multipart.max-request-size=50MB
# T?o thêm property tùy ch?nh cho ???ng d?n l?u file
# ???ng d?n l?u file nh?c (ví d?: th? m?c music-uploads cùng c?p v?i file jar)
file.upload-dir=G:/ImageFile/music-app-uploads/uploads-videos/

# --- Server Configuration ---
# C?ng ch?y ?ng d?ng (m?c ??nh là 8080)
server.port=8080 
# (Tùy ch?n) ??t context path cho ?ng d?ng
server.servlet.context-path=/musicapp

# ---- NoHandlerFoundException ----
# C?n thi?t n?u b?n không dùng mapping resource m?c ??nh
spring.web.resources.add-mappings=false

# --- Logging (Optional Example) ---
logging.level.root=INFO
logging.level.com.example.musicapp=DEBUG
logging.file.name=./logs/musicapp.log # Ch? ??nh file log
# logging.pattern.console=... # ??nh d?ng console
# logging.pattern.file=...    # ??nh d?ng file


